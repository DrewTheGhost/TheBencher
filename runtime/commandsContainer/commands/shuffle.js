module.exports = {
    name: "shuffle",
    aliases: [""],
    description: "Shuffles the queue!",
    controlled: false,
    fn(message, suffix, bot, db) {
        // TODO, old code that doesn't work
    }
} 